- defaults:
    link:
      create: true
      relink: true
      force: true

- clean: ['~', '~/.config']

- shell:
  - [ cp ./fonts ~/.local/share/ -r && fc-cache, copying fonts...]

- link:
    #~/.config/alacritty: alacritty
    #~/.config/picom: picom
    #~/.config/wofi: wofi
    #~/.config/wayfire.ini: wayfire.ini
    #~/.config/sway: sway
    #~/.config/xmonad: xmonad
    #~/.xmobarrc: xmobar/.xmobarrc
    #~/.gitconfig: .gitconfig
    ~/.config/hypr: hypr
    #~/.config/waybar: waybar
    ~/.config/eww: eww
    ~/.config/nvim: nvim
    #~/.config/syncthing: syncthing
    ~/.config/Typora/themes: typora-theme
    ~/.local/share/fcitx5/rime/default.custom.yaml: rime/default.custom.yaml

- shell:
  - [git submodule update --init --recursive, Installing submodules]

